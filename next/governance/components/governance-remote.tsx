import type { PlatformContext } from '@pilot/contracts';
import { Button } from './ui/button';
export default function GovernanceRemote({ platform, summary, onHello }: { platform: PlatformContext; summary: Record<string, number> | null; onHello: () => void }) { console.info('[next-governance-remote] platform-context-received', { userId: platform.user.id, tenantId: platform.tenant.id }); return <section data-testid="next-governance-remote" className="domain-view"><p className="eyebrow">Security / {platform.tenant.name} / HTTP Remote</p><h2>Policy ledger</h2>{summary ? <><div className="metrics"><div><strong>{summary.policiesPassing}</strong><span>policies passing</span></div><div><strong>{summary.exceptions}</strong><span>open exception</span></div><div><strong>{summary.auditEvents}</strong><span>audit events</span></div></div><div className="action"><Button data-testid="next-governance-hello" onClick={onHello}>Send hello to Security</Button></div></> : <p className="outlet-state">Reconciling the policy ledger…</p>}</section>; }
